{
  "name": "bar.io",
  "scripts": {
    "postinstall": "run-p -l backend:install menu:install",
    "backend:install": "cd backend && npm install",
    "menu:install": "cd menu-public-portal && npm install"
  },
  "devDependencies": {
    "husky": "^7.0.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.5.1",
    "pretty-quick": "^3.1.3"
  }
}
